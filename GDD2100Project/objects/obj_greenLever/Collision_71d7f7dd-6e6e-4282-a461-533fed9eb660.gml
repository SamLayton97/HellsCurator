/// @description When player collides with this lever

// if not already active, make it so
if (!active)
{
	active = true;
	
	// TODO: set sprite to active and play sound effect
	sprite_index = spr_greenLeverActive;
}